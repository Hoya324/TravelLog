<!DOCTYPE html>
<html>
<head>
  <meta charset="UTF-8" />
  <meta http-equiv="X-UA-Compatible" content="IE=edge" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link rel="stylesheet" href="/list.css" />
  <link rel="stylesheet" href="/header.css" />
  <title>여행 기록 페이지</title>
</head>
<body>
<%- include('./header') %>
<div class="wrapp">
  <div class="wrap">
    <div style="overflow: scroll" class="list_wrap">
      <div class="list_area">
        <% list.reverse().forEach((travel) => { %>
          <p>
            <a href="/record/<%= travel.dataValues.id %>">
              <%= travel.dataValues.name %> | <%= travel.dataValues.address ? travel.dataValues.address : '(주소 미제공)' %>
            </a>
            <button class="remove_button" onclick="removeTravel('<%= travel.dataValues.TravelId %>')">
              <img class="remove" src="/trash.png" alt="" />
            </button>
          </p>
          <hr />
        <% }); %>
      </div>
    </div>
  </div>
</div>
</body>
<script src="/list.js"></script>
<script src="https://unpkg.com/axios/dist/axios.min.js"></script>
</html>
